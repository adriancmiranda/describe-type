toc:
  - name: as
    description: |
      Evaluates whether an expression specified by the second operand is a
      member of the data type specified by the first operand.
  - as / a / an / type
  - any
  - instanceOf
  - arrayOf

  - name: has
    description: |
      Evaluates whether an expression specified by the second operand is a
      member of the data type specified by the first operand.
  - at
  - own
  - own.property
  - own.value
  - unsafeMethod

  - name: internal
    description: |
      Evaluates whether an expression specified by the second operand is a
      member of the data type specified by the first operand.
  - apply
  - booleanOf
  - built-in
  - constants
  - constructorNameOf
  - constructorOf
  - each
  - each.property
  - each.value
  - env
  - floatOf
  - getExpectedValue
  - intOf
  - mod
  - name
  - patterns
  - prototypeOf
  - prototypes
  - resolveProperty
  - slice
  - stringify
  - stringOf
  - typeOf
  - typify
  - uintOf

  - name: is
    description: |
      Evaluates whether an expression specified by the second operand is a
      member of the data type specified by the first operand.
  - args
  - args.empty
  - array
  - array.of
  - array.empty
  - arraylike
  - arraylike.empty
  - fn.callable
  - fn.caste
  - fn.native
  - fn
  - not
  - not.a / not.an / not.type
  - not.any
  - not.instanceOf
  - not.arrayOf
  - object
  - object.empty
  - regexp
  - regexp.flags
  - regexp.string
  - stream
  - stream.duplex
  - stream.readable
  - stream.transform
  - stream.writable
  - string
  - string.empty
  - a / an / type
  - any
  - base64
  - bool
  - buffer
  - callable
  - date
  - decimal
  - defined
  - element
  - empty
  - enumerable
  - equal
  - error
  - even
  - exotic
  - filled
  - hex
  - hexadecimal / hex.decimal?
  - hosted
  - infinity
  - instanceOf
  - int / integer?
  - jsonlike
  - max
  - min
  - nan
  - nil
  - number
  - numeric
  - odd
  - primitive
  - symbol
  - uint / unsignetInteger?
  - undef
  - unfilled
  - within

  - name: ponyfill
    description: |
      Evaluates whether an expression specified by the second operand is a
      member of the data type specified by the first operand.
  - Array.prototype.filter
  - Array.prototype.reduce
  - Object.assign
  - Object.is
  - Object.keys
  - String.prototype.startsWith

  - name: schema
    description: |
      Evaluates whether an expression specified by the second operand is a
      member of the data type specified by the first operand.
  - schema

  - name: shim
    description: |
      Evaluates whether an expression specified by the second operand is a
      member of the data type specified by the first operand.
  - Object.create
  - Object.getPrototypeOf
